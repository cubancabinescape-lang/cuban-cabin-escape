document.getElementById('year').textContent = new Date().getFullYear();
const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav');navToggle?.addEventListener('click',()=>nav.classList.toggle('open'));
const guests=document.getElementById('guests'), nights=document.getElementById('nights'), estimate=document.getElementById('estimate');function calc(){const g=Math.max(1,Number(guests.value||1));const n=Math.max(1,Number(nights.value||1));estimate.textContent='$'+(g*n*50).toLocaleString();}guests?.addEventListener('input',calc);nights?.addEventListener('input',calc);calc();
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
document.querySelectorAll('.gallery img').forEach(img=>{img.addEventListener('click',()=>{const overlay=document.createElement('div');overlay.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.88);z-index:100;display:grid;place-items:center;padding:24px;cursor:pointer';const clone=img.cloneNode();clone.style.cssText='max-height:90vh;max-width:95vw;border-radius:20px;object-fit:contain';overlay.appendChild(clone);overlay.addEventListener('click',()=>overlay.remove());document.body.appendChild(overlay);});});
